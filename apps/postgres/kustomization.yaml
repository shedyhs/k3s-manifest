apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

labels:
  - pairs:
      app: postgres

resources:
  - statefulset.yaml
  - service.yaml

secretGenerator:
  - name: postgres-secret
    envs:
      - postgres.env
